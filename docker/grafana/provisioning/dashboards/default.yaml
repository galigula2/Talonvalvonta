apiVersion: 1

providers:
  - name: Default    # A uniquely identifiable name for the provider
    folder: Services # The folder where to place the dashboards, cannot be "General" as that already exists
    type: file
    options:
      path: /var/lib/grafana_dashboards